# ──────────────────────────────────────────────────────────────
# 사업비 전표 분析 도구 — Python 의존성 목록
#
# 설치 방법:
#   pip install -r requirements.txt
#
# 실행 환경:
#   - Python 3.10 이상 (권장: 3.11+)
#   - Windows (맑은 고딕 폰트 필요 — C:\Windows\Fonts\malgun.ttf)
#   - tkinter: Python 설치 시 기본 포함 (별도 pip 불필요)
# ──────────────────────────────────────────────────────────────

# 데이터 처리
pandas>=2.0.0
openpyxl>=3.1.0

# PDF 생성 (ReportLab — 순수 Python, 한국어 TTFont 지원)
reportlab>=4.0.0

# 이미지 처리 (로고 PNG 합성 — pdf_exporter.py 필수 의존성)
Pillow>=10.0.0

# 설정 파일 파싱 (column_config.yaml — 컬럼·시트명 외부화)
pyyaml>=6.0
